{
  "name": "mercedes-sdv-presentation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate-slides": "node scripts/generate-slides.js",
    "predev": "npm run generate-slides",
    "dev": "vite",
    "prebuild": "npm run generate-slides",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build && cp -r dist/* docs/"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.2.0"
  }
}
